import DefaultProvider from "./defaultProvider"

class NimbellaProvider extends DefaultProvider {
    
    constructor() {
        super();
        this._name = "nim";
      }
};

export default NimbellaProvider;

